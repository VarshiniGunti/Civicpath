# No Gemini API key needed — Vertex AI uses Application Default Credentials (ADC)
# Locally: run `gcloud auth application-default login` once
# On Cloud Run: ADC is automatic via the compute service account

GCP_PROJECT_ID=votesathi-ai
PORT=8080
